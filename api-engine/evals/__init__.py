"""Measuring a bot against a fixed set of questions.

A model change is only an improvement if it is measured on the same questions
as before. Run a set with ``python -m evals <set.json>`` from api-engine/; see
docs/evaluation.md.
"""
